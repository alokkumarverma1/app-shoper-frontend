import 'package:flutter/material.dart';

class MyRevenue extends StatelessWidget {
  const MyRevenue({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SafeArea(
          child: CustomScrollView(

          )
      ),
    );
  }
}
